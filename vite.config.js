import { defineConfig } from 'vite';

export default defineConfig({
  // Domínio definitivo ainda não definido: base relativa permite servir o
  // build de qualquer caminho durante a validação.
  base: './',
});
